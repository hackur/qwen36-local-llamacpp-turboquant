# Terminal chat

`make demo` connects to `qwen3.8-local` on `:10501`. The client supports
streaming, bounded history, thinking toggles, save/load, and clear/help
commands. Use the native WebUI for image attachments and agent tools.
