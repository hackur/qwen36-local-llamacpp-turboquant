{
  "name": "qwen-compact-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Local compaction proxy for the Qwen3.8 / TurboQuant / llama.cpp stack.",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "js-yaml": "^5.3.0",
    "pino": "^10.3.1"
  }
}
